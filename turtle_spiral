import turtle
t=turtle.Turtle()
t.speed(10)
colors = ["red", "blue", "green", "orange", "purple"]
b = 0
while b<1000:
  
  for a in colors:  
    t.color(a) 
    t.forward(b)
    t.right(100)
    b = b+1
    
